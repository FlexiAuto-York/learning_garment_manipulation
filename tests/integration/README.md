# Integration tests

Subsystem interactions across the `registration/` boundary: perception → planning,
planning → control, agent → arena.

Requires a built PyFlex and a GPU, so these do not run in CI. See `../README.md`.

Placeholder — the arena tests currently in `tests/unit/` belong here once made self-contained.
